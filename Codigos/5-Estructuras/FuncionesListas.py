"""
    Entre las instrucciones más importantes que tenemos para manejar listas,
    se encuentran:
        lista.append(elemento)
        lista.index(elemento)
        lista.count(elemento)
        lista.remove(elemento)
        min(lista)
        max(lista)
        sorted(lista)

    Además, para acceder a un elemento especifico de la lista, utilizamos la siguiente
    instrucción
        lista[indice del elemento a consultar]
"""

numeros = [5,3,9,83,3,4,3,16,2,6,5,3,7,6,8,10,11,123,54,32]

numeros.append(150)

#print(numeros)
#print(numeros.index(150))

#print(numeros.count(3))

#numeros.remove(150)

print(numeros[0])